mutation createPatreon($id: ID!,$username: String!, $campaignId: ID!) {
    createPatreon(creatorId: $id, patreon: {username: $username, campaignId: $campaignId}) {
        creator {
            id
        }
    }
}

